# Runtime dependencies for deployment. Mirrors [project.dependencies] in
# pyproject.toml, which stays the source of truth for local development
# (`pip install -e ".[dev]"`).
#
# Vercel's Python runtime installs from this file only.
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
sqlalchemy[asyncio]>=2.0.36
asyncpg>=0.30.0
alembic>=1.14.0
pydantic>=2.10.0
pydantic-settings>=2.7.0
email-validator>=2.2.0
pyjwt>=2.10.0
argon2-cffi>=23.1.0
python-multipart>=0.0.20
httpx>=0.28.0
boto3>=1.35.0
google-genai>=1.0.0
openpyxl>=3.1.0
cryptography>=43.0.0
